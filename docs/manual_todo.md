# Final manual publication tasks

The analytical pipeline and six-page Power BI report are complete. The remaining steps require visual review or repository-owner decisions.

- Export the six approved Power BI pages to `images/` using the filenames in [images/README.md](../images/README.md).
- Add the Executive Overview image, and optionally a compact gallery, to the README after verifying the images.
- In Power BI Desktop, update `pProjectRoot` to the cloned repository path before refresh.
- Optionally hide the technical `PBIR Templates` page in Desktop; it was left unchanged to avoid unsupported PBIR edits.
- Choose a software license for the author's original code. Dataset/source licensing remains governed by the original publishers.
- Review `git status`, the tracked prototype deletions, and all untracked analytical assets before staging.
- Create/configure the GitHub remote only after local review.
